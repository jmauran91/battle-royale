import React from 'react';
import GameContainer from 'react/containers/GameContainer';
import path from 'path';

describe('Testing_game', () => {
  let score, card, header, button;
  var player_num = 

  beforeEach(() => {
    wrapper = mount(
      <GameContainer
      score=""
      card=""
      header=""
      button=""
    )
  })
  it('should say battle royale', () => {

  });
  it('should give the user option to start game', () => {

  });
  it('screen should show score', () => {

  });
  it('image of each card should show', () => {
    expect().toBe();
  });
  it('')
})
